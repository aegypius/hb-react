Harp boilerplate for a React App (using babel + browserify)

first:

```
$ harp init -b mrmcc3/hb-react myReactApp
$ cd myReactApp
$ npm install
```

then for dev:

```
$ npm run dev
```

then for dist:

```
$ npm run dist
```
